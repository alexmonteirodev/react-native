No app ao criar uma pasta como movies e dentro dela inserir [id].tsx, criamos uma rota dinamica, agora basta criar o link no index:

index:

import { Text, View } from "react-native";
import { Link } from "expo-router";

export default function Index() {
  return (
    <View className="flex-1 justify-center items-center">
      <Text className="text-light-100">Welcome!</Text>
      <Link href={"/onboarding"}>onboarding</Link>
      <Link href={"/movie/avengers"}>Avenger Movie</Link>
    </View>
  );
}

e usar o useLocalSearchParams(), desestruturando o id no arquivo [id].tsx para ter acesso à rota dinamica:

import { StyleSheet, Text, View } from "react-native";
import React from "react";
import { useLocalSearchParams } from "expo-router";

const Details = () => {
  const { id } = useLocalSearchParams();
  return (
    <View>
      <Text>Movie details: {id}</Text>
    </View>
  );
};

export default Details;

const styles = StyleSheet.create({});
